<div class="container marketing">
  <div class="row d-flex justify-content-center">
    <div class="col-10 mt-5">
      <h1>FOLLOWERS</h1>
      <table class="table text-center">
        <thead>
          <tr>
            <th>profile image</th>
            <th>
              <div style="margin-left: -100px;">Name</div>
            </th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td>
                <% if user.profile_image.attached? %>
                  <%= image_tag user.profile_image.variant(resize_to_fit: [50, 50]), class: "is-rounded", width: 50, height: 50 %>
                <% else %>
                  <%= image_tag "default-image.jpg", class: "is-rounded", width: 50, height: 50 %>
                <% end %>
              </td>
              <td>
                <div class="cell-content" style="margin-top: 13px; margin-bottom: 10px; margin-left: -100px;">
                  <%= link_to user_path(user) do %>
                    <%= user.name %>
                  <% end %>
                </div>
              </td>
              <td>
                <div class="cell-content" style="margin-top: 10px; margin-bottom: 10px;">
                <% if user != current_user %>
                  <% if user.is_followed_by?(current_user) %>
                    <%=link_to user_relationships_path(user), method: :delete do %>
                      <button type="button" class="btn btn btn-danger">フォロー解除</button>
                    <% end %>
                  <% else %>
                    <%=link_to user_relationships_path(user), method: :post do %>
                      <button type="button" class="btn btn btn-primary">フォロー</button>
                    <% end %>
                  <% end %>
                <% end %>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>




